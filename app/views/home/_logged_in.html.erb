<h2 class='cate-a'>カテゴリーを選択してください</h2>
<table>
    <thead>
        <% @spot_categories.each do |category|%>
          <div class="cate-b">
          <%= link_to "#{category.name}", "#{category.url_name}", class: 'link'  %>
          </div>
        <% end %>
    </thead>
</table>
<div class='cate-c'>
  <%= link_to "新規投稿", post_path, class: 'link' %>
</div>